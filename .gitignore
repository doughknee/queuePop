__pycache__/
*.pyc
build/
dist/
releases/
.vscode/
.idea/
venv/
.env

# Runtime artifacts (may contain secrets like Discord webhooks)
config.json
stats.json
*.log

# Downloaded League assets — regenerated via scripts/fetch_assets.py
src/webui/assets/champions/
src/webui/assets/positions/
src/webui/assets/skins/
src/webui/assets/spells/
# Runtime asset refresh (About → Refresh champion data, src/asset_refresh.py)
src/assets_override/

# Node tooling for the Tailwind build (CSS itself is committed)
node_modules/

# Local Claude Code tooling (settings, worktrees) — not for the repo
.claude/
scripts/config_before.json

# Counter-engine analysis outputs (from retired tuning scripts; kept ignored
# so old local runs don't show up as untracked noise)
data/coverage_report.json
data/spotlight_report.json
data/recommend_pct.json
data/stuck_champs.json
